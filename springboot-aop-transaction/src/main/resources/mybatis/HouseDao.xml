<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.dao.HouseDao">


    <!--通过主键修改数据-->
    <update id="updateHouse">
        update house set title = '华瑞紫银华庭'   where id = #{id}
    </update>
    <select id="findAll" resultType="com.example.demo.entity.House">
        select * from house
    </select>
    <select id="findAllByPage" resultType="com.example.demo.entity.House">
        select * from house
    </select>

</mapper>